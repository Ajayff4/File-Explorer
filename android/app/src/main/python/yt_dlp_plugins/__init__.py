"""Vendored yt-dlp plugin namespace package.

`__init__.py` is required: Chaquopy's import machinery does not handle
PEP 420 namespace packages, so the plugin is shipped as a regular package
here (in app.imy) instead of via `pip install` of the upstream zip.
"""